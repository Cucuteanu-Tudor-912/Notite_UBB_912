## Declare
`$variable_name d# $value` where # is dataType
#### Byte
`db` - 8 bits
#### Word
`dw` - 16 bits
#### DoubleWord
`dd` - 32 bits
#### QuadWord
`dq` - 64 bits

#### Arrays
`d# $v1, $v2, $v3, ...`

#### Data address
`0x000000`